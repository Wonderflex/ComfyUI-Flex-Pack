# Known Issues
This is a log of known issues that I'll work through as time allows.

## XY Grid Engine ##
- Really axis labels  shrink too small and should instead wrap at some point
- When only one variable changes in a grid it still reruns all images.  It should only run the new images. 

